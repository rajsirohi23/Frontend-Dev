var count = 0;
function increment() {
    count = count + 1; // we are adding 1 here each time
    console.log(count);
}

function decrement() {
    count = count - 1; // we are subtracting 1 here each time
    console.log(count);
}
increment();
increment();
decrement();
